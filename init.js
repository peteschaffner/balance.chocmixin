/**
 * Balance command for HTML/CSS/JS
 *
 * Modeled after Espresso.app's Balance implementation
 */

Hooks.addMenuItem("Text/Select/Balance", "control-b", function () {
  Alert.show('Balance', 'Balancing like a mofo', ['Sweet!']);
});